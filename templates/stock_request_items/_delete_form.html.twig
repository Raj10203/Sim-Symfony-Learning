<form method="post" action="{{ path('app_stock_request_items_delete', {'id': stock_request_item.id}) }}"
      id="delete_stock_request_item"
      class="delete-form" data-product-name="{{ stock_request_item.product.name|default('this item') }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_request_item.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
