<form method="post" action="{{ path('app_stock_movement_delete', {'id': stock_movement.id}) }}"
      id="delete_stock_movement_form"
      class="delete-form" data-product-name="{{ stock_movement.id|default('this item') }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_movement.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
