<form method="post" action="{{ path('app_retired_inventory_delete', {'id': retired_inventory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retired_inventory.id) }}">
    <button class="btn">Delete</button>
</form>
