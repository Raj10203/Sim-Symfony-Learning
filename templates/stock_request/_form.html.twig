<div class="container-fluid">
    {{ form_start(stock_request_form) }}
    <div class="row">
        <div class="col-md-4 col-12 mb-3">
            <div class="form-group">
                {{ form_row(stock_request_form.status) }}
            </div>
        </div>
        <div class="col-md-4 col-12 mb-3">
            <div class="form-group">
                {{ form_label(stock_request_form.fromSite) }}
                {{ form_widget(stock_request_form.fromSite) }}
            </div>
        </div>
        <div class="col-md-4 col-12 mb-3">
            <div class="form-group">
                {{ form_label(stock_request_form.toSite) }}
                {{ form_widget(stock_request_form.toSite) }}
            </div>
        </div>
    </div>
    <div class="d-flex">
        <div class="mt-3">
            {{ form_widget(stock_request_form._token) }} {# optional hidden CSRF field #}
        </div>
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
        {{ form_end(stock_request_form) }}
        {% if button_label is defined %}
            {{ include('stock_request/_delete_form.html.twig') }}
        {% endif %}
    </div>
</div>

